# このリポジトリについて

柴田篤志の実験・スクリプト置き場(monorepo的運用)。文章コンテンツ(日本語)と、AI エージェントの生産性設定も管理する。

このファイルは **Claude Code と Codex の共有指示**。両者が同じ前提で動くための単一の情報源。ツール固有の設定は各自のファイルに書く(Claude Code は `CLAUDE.md`)。

## ディレクトリ構成

- `/scripts` — 単発Python・セットアップスクリプト
- `/analysis` — 動作分析・HealthKit
- `/experiments` — 試行
- `/docs` — ドキュメント・文章コンテンツ

## コーディング規約

- Pythonは3.11想定
- コメント・docstringは日本語で書く

## 原則(スリムに保つこと)

- 回答・コミットメッセージ・ドキュメントは日本語で書く。
- 曖昧な依頼は着手前にゴール・制約・完了条件を1度だけ確認する。
- 3つ以上の独立した調査・作業は並列化を検討する。ただし互いに依存する作業は並列化しない(かえって遅く、質も落ちる)。同時に走らせるのは最大3〜5。
- 事実確認を怠らない。推測で断定せず、確かめられることは確かめてから書く。

## ブランチ運用

- 破壊的変更は必ずブランチを切り、mainに直接pushしない。

## エージェントの役割分担

Claude Code と Codex を併用する。**併用の価値はスループットではなく視点の違い**にある。

| | 役割 |
|---|---|
| **Claude Code** | メインループ。計画・実装・統合・最終判断 |
| **Codex** | セカンドオピニオン。批判的レビュー・独立検証・詰まったときの別解 |

- **最終判断は常に Claude Code 側**。Codex の指摘は「採用/不採用を理由つきで決める入力」であって、自動的に従うものではない。
- 同じ作業を両者に並行させない。役割で分ける。
- 詳細は `docs/codex-integration-guide.md`。
