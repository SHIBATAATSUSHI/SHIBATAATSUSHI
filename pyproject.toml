[project]
name = "shibataatsushi"
version = "0.1.0"
description = "柴田篤志の実験・スクリプト置き場"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["scripts"]
